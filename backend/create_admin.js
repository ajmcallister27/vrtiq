import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); const email = "test@test.com"; const user = await prisma.user.findUnique({ where: { email } }); if (!user) { console.log("User not found:", email); } else { await prisma.user.update({ where: { email }, data: { role: "admin" } }); console.log("Promoted to admin:", email); } await prisma.$disconnect();